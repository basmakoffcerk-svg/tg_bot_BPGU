# Отчет об исследовании состояния репозитория (Repository Survey Report)

**Дата обследования:** 2026-09-08  
**Агент:** `explorer_survey_2`  
**Целевой проект:** АРМ Старосты («Пульт управления группой 240326 Матинф БГПУ»)  
**Рабочая директория:** `/Users/sergei/Desktop/tg_bot`

---

## 1. Текущее состояние файловой структуры репозитория

### 1.1. Фактический состав файлов в корневой директории
При сканировании корневой директории `/Users/sergei/Desktop/tg_bot` обнаружены следующие файлы и каталоги:

| Имя файла / папки | Тип | Размер | Назначение / Описание |
|---|---|---|---|
| `docs/` | Директория | 5 файлов | Полный комплект спецификаций и архитектурной документации |
| `docs/SRS.md` | Файл | 17 916 байт | Техническое задание (Software Requirements Specification) |
| `docs/ARCHITECTURE.md` | Файл | 14 533 байт | C4 диаграммы, sequence диаграммы, HMAC алгоритм, структура папок |
| `docs/DATABASE.md` | Файл | 10 821 байт | ERD, DDL SQL схемы 6 таблиц, WAL прагмы, регламент бэкапов |
| `docs/API.md` | Файл | 8 930 байт | Контракты REST API (JSON схемы, статус-коды, RFC 7807) |
| `docs/DEPLOYMENT.md` | Файл | 8 043 байт | Руководство по развертыванию, листинги Dockerfile и docker-compose |
| `requirements.txt` | Файл | 498 байт | Список производственных Python-зависимостей |
| `.env.example` | Файл | 1 176 байт | Шаблон конфигурации переменных окружения |
| `README.md` | Файл | 7 437 байт | Презентационное описание проекта и дорожная карта (Roadmap) |
| `ORIGINAL_REQUEST.md` | Файл | 6 767 байт | Исходное задание пользователя и критерии приемки |
| `.agents/` | Директория | Метаданные | Служебные метаданные мультиагентной системы |
| `.DS_Store` | Файл | 6 148 байт | Служебный файл macOS Finder |

### 1.2. Git-статус
- Вызов команды `git status` возвращает:
  ```
  fatal: not a git repository (or any of the parent directories): .git
  ```
- В репозитории **не инициализирована система контроля версий Git** (отсутствует каталог `.git`).
- Отсутствует файл `.gitignore`.

---

## 2. Аудит среды выполнения Python и зависимостей

### 2.1. Системный интерпретатор
- Команда `which python3` указывает на системный интерпретатор: `/usr/bin/python3` (версия `Python 3.9.6`).
- **Несоответствие спецификации:** в `docs/ARCHITECTURE.md:5` и `README.md:3` жестко зафиксировано требование **Python 3.11+**. Версия Python 3.9.6 не удовлетворяет современным требованиям типизации (`|` union syntax, Pydantic v2 modern features, asyncio task groups).

### 2.2. Менеджер пакетов и окружений `uv`
- В системе установлен современный высокопроизводительный менеджер пакетов `uv`:
  - Путь: `/usr/local/bin/uv`
  - Версия: `uv 0.11.31 (Homebrew 2026-07-22 x86_64-apple-darwin)`
- Команда `uv python list` показала наличие в кэше:
  - `cpython-3.10.20-macos-x86_64-none` по пути `/Users/sergei/.local/share/uv/python/cpython-3.10-macos-x86_64-none/bin/python3.10`
  - Возможность загрузки и автоматической установки `cpython-3.11.15-macos-x86_64-none` и `cpython-3.12.13-macos-x86_64-none` одной командой (например, `uv venv --python 3.11 .venv`).

### 2.3. Анализ и разрешение зависимостей `requirements.txt`
Выполнена компиляция графа зависимостей через `uv pip compile requirements.txt`.  
Граф из **51 пакета успешно разрешен за 1.64 сек** без единого конфликта версий:

| Пакет | Требование в `requirements.txt` | Разрешенная версия | Статус |
|---|---|---|---|
| `fastapi` | `>=0.110.0,<0.111.0` | `0.110.3` | ✅ Валидно |
| `uvicorn[standard]` | `>=0.28.0,<0.29.0` | `0.28.1` | ✅ Валидно |
| `pydantic` | `>=2.6.4,<3.0.0` | `2.13.5` | ✅ Валидно |
| `pydantic-settings` | `>=2.2.1,<3.0.0` | `2.15.0` | ✅ Валидно |
| `aiogram` | `>=3.4.1,<4.0.0` | `3.31.0` | ✅ Валидно |
| `sqlalchemy` | `>=2.0.28,<2.1.0` | `2.0.52` | ✅ Валидно |
| `aiosqlite` | `>=0.20.0,<0.21.0` | `0.20.0` | ✅ Валидно |
| `alembic` | `>=1.13.1,<1.14.0` | `1.13.3` | ✅ Валидно |
| `openpyxl` | `>=3.1.2,<3.2.0` | `3.1.5` | ✅ Валидно |
| `reportlab` | `>=4.1.0,<5.0.0` | `4.5.1` | ✅ Валидно |
| `apscheduler` | `>=3.10.4,<4.0.0` | `3.11.3` | ✅ Валидно |
| `httpx` | `>=0.27.0,<0.28.0` | `0.27.2` | ✅ Валидно |
| `pytz` | `>=2024.1` | `2026.3.post1` | ✅ Валидно |
| `python-multipart` | `>=0.0.9` | `0.0.32` | ✅ Валидно |

### 2.4. Выявленные пробелы в зависимостях (Missing Dependencies)
В файле `requirements.txt` **отсутствуют зависимости для тестирования**:
1. `pytest` (необходим для выполнения критерия приемки R-AC: автоматические тесты).
2. `pytest-asyncio` (критически необходим для тестирования асинхронных эндпоинтов FastAPI и обработчиков aiogram 3.x).
3. `pytest-mock` (для мокирования ответов Telegram Bot API и внешних сервисов).
4. `coverage` (для контроля покрытия кода тестами).

Рекомендация: сформировать `requirements-dev.txt` или расширить `requirements.txt` секцией для разработки/тестов.

---

## 3. Наличие мок-файлов, скелетов кода и БД

| Компонент | Наличие на диске | Пояснение |
|---|---|---|
| База данных SQLite (`data/database.sqlite`) | ❌ Отсутствует | Директория `data/` и сам файл БД отсутствуют |
| Скрипты миграций (`alembic/`) | ❌ Отсутствует | Конфигурация `alembic.ini` и каталог миграций не инициализированы |
| Файл вайтлиста студентов (Группа 240326) | ❌ Отсутствует | Нет ни `.json`, ни `.csv`, ни `.py` сид-файла со списком студентов |
| Шаблон расписания занятий БГПУ | ❌ Отсутствует | Нет сид-данных для слотов пар, координат корпусов БГПУ |
| Исходный код бэкенда (`api/`, `bot/`, `core/`, `models/`, `services/`, `main.py`) | ❌ Отсутствует | Ни один модуль бэкенда еще не реализован |
| Фронтенд Telegram Mini App (`webapp/`) | ❌ Отсутствует | HTML, CSS, JS файлы Mini App отсутствуют |
| Тесты (`tests/`) | ❌ Отсутствует | Каталог тестов и тест-кейсы отсутствуют |
| Файлы контейнеризации (`Dockerfile`, `docker-compose.yml`) | ❌ Отсутствует | Листинги есть в документации `DEPLOYMENT.md`, но физических файлов нет |
| Файл рабочей конфигурации `.env` | ❌ Отсутствует | Присутствует только образец `.env.example` |

---

## 4. Детальный анализ несоответствий (Gap Analysis) между `docs/` и репозиторием

Вся кодовая база проекта на данный момент находится на **Этапе 0** («Согласование архитектуры и написание полной документации»), как зафиксировано в `README.md:76`:
```
[x] Этап 0: Согласование архитектуры и написание полной документации (SRS, ARCH, DB, API, DEPLOY)
[ ] Этап 1: Инициализация репозитория, настройка окружения, моделей SQLAlchemy и миграций Alembic
[ ] Этап 2: Разработка асинхронного REST API (FastAPI) и валидации initData
[ ] Этап 3: Создание Telegram-бота на aiogram 3.x (онбординг по вайтлисту, подтверждение старостой)
[ ] Этап 4: Верстка Telegram Mini App (карточки расписания, геолокация, интерактивная шахматка)
[ ] Этап 5: Реализация генератора рапортички в Excel (.xlsx) и фонового планировщика
[ ] Этап 6: Сквозное тестирование и боевой деплой
```

### Специфические пробелы в документации и данных, требующие внимания до начала кодинга:
1. **Отсутствие эталонного списка группы 240326:**
   В `docs/SRS.md:58-61` сказано: *«В базу данных изначально загружается официальный списочный состав группы 240326 (ФИО, подгруппа 1 или 2)»*. Сам состав фамилий в документации не приведен (есть только единичные примеры «Иванов Иван Иванович», «Александров Александр», «Борисов Борис», «Волков Владимир»). Для полноценной разработки и тестирования необходим сид-файл на 25–30 студентов группы 240326 «Матинф».
2. **Координаты корпусов БГПУ:**
   В `docs/API.md:106` и `132` в качестве координат корпусов указаны тестовые московские координаты (lat: 55.751244, lon: 37.618423), хотя проект предназначен для БГПУ им. Максима Танка (Минск, пл. Независимости, координаты ~53.8943, 27.5458). Сид-генератор должен поддерживать как реальные координаты БГПУ, так и тестовые для прогона юнит- и E2E-тестов.
3. **Объединение процессов FastAPI и aiogram:**
   Согласно `docs/ARCHITECTURE.md:11`, система строится как модульный монолит, где FastAPI и aiogram запускаются в едином процессе под Uvicorn. Рекомендуется использовать `lifespan` FastAPI для старта/остановки бота (webhook или polling) и планировщика APScheduler.

---

## 5. Рекомендуемая файловая структура проекта

На основе `docs/ARCHITECTURE.md:214-253` и стандартов промышленной разработки на FastAPI + aiogram 3.x рекомендуется следующая компоновка репозитория:

```
tg_bot/
├── .env.example                     # [Существует] Пример конфигурации
├── .env                             # [Создать] Локальная конфигурация
├── .gitignore                       # [Создать] Исключение venv, sqlite, __pycache__, logs
├── requirements.txt                 # [Существует] Продакшн-зависимости
├── requirements-dev.txt             # [Создать] pytest, pytest-asyncio, pytest-mock, coverage
├── pyproject.toml                   # [Создать] Настройки pytest, ruff, black
├── Dockerfile                       # [Создать] На базе docs/DEPLOYMENT.md:109
├── docker-compose.yml               # [Создать] На базе docs/DEPLOYMENT.md:141
├── alembic.ini                      # [Создать] Конфигурация миграций Alembic
├── main.py                          # [Создать] Точка входа: FastAPI lifespan (Uvicorn + aiogram + scheduler)
│
├── alembic/                         # [Создать] Миграции базы данных
│   ├── env.py
│   ├── script.py.mako
│   └── versions/
│
├── core/                            # [Создать] Системное ядро
│   ├── __init__.py
│   ├── config.py                    # pydantic-settings Settings (.env)
│   ├── security.py                  # Валидация HMAC-SHA256 Telegram initData
│   └── database.py                  # SQLAlchemy 2.0 AsyncEngine, Base, get_async_session
│
├── models/                          # [Создать] Декларативные SQLAlchemy 2.0 ORM-модели
│   ├── __init__.py                  # Экспорт всех моделей для Alembic
│   ├── base.py                      # DeclarativeBase, TimestampMixin
│   ├── student.py                   # Таблица students (RBAC, telegram_id, subgroup)
│   ├── subject.py                   # Таблица subjects
│   ├── schedule.py                  # Таблицы schedule_slots и pairs_registry
│   ├── attendance.py                # Таблица attendance
│   ├── broadcast.py                 # Таблица broadcast_messages
│   └── audit.py                     # Таблица audit_log
│
├── schemas/                         # [Создать] Pydantic v2 схемы данных (DTO)
│   ├── __init__.py
│   ├── auth.py                      # AuthResponse, UserProfile, CurrentWeek
│   ├── schedule.py                  # ScheduleDayResponse, PairCardResponse
│   ├── attendance.py                # CheckinRequest, CheckinResponse, GridResponse, OverrideRequest
│   ├── alert.py                     # BroadcastRequest, BroadcastResponse
│   └── report.py                    # ExportReportRequest, ExportReportResponse
│
├── services/                        # [Создать] Бизнес-логика (чистые сервисы)
│   ├── __init__.py
│   ├── geo_service.py               # Вычисление дистанции (Haversine formula), accuracy check
│   ├── attendance_service.py        # Логика чекина, временные окна, шахматка, оверрайды, лок
│   ├── excel_generator.py           # Генерация официальной рапортички деканата (openpyxl)
│   ├── broadcaster.py               # Сервис экстренной рассылки (@all + личные сообщения)
│   ├── scheduler.py                 # Фоновые задачи APScheduler (субботний автоотчет)
│   └── seeder.py                    # Начальная инициализация вайтлиста 240326 и расписания
│
├── api/                             # [Создать] Слой REST API (FastAPI)
│   ├── __init__.py
│   ├── app.py                       # FastAPI экземпляр, CORS, lifespan, exception handlers
│   ├── dependencies.py              # Auth initData dependency, require_roles (RBAC)
│   └── routes/
│       ├── __init__.py
│       ├── health.py                # GET /api/v1/health
│       ├── auth.py                  # POST /api/v1/auth/telegram
│       ├── schedule.py              # GET /api/v1/schedule/today
│       ├── attendance.py            # POST /checkin, GET /grid/{id}, PATCH /override, POST /lock/{id}
│       ├── alerts.py                # POST /api/v1/alerts/broadcast
│       └── reports.py               # POST /api/v1/reports/export
│
├── bot/                             # [Создать] Слой Telegram-бота (aiogram 3.x)
│   ├── __init__.py
│   ├── bot.py                       # Bot, Dispatcher, Router
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── start.py                 # /start, онбординг, выбор ФИО из вайтлиста
│   │   ├── admin.py                 # Инлайн-подтверждение старостой заявок студентов
│   │   ├── report.py                # /report команда выгрузки ведомости
│   │   └── common.py                # /help, /status
│   ├── keyboards/
│   │   ├── __init__.py
│   │   ├── inline_onboarding.py     # Инлайн-клавиатуры выбора ФИО и одобрения старостой
│   │   └── reply_menu.py            # Главное меню, кнопка вызова Mini App
│   └── middlewares/
│       ├── __init__.py
│       └── db_session.py            # Инъекция сессии БД в хэндлеры aiogram
│
├── webapp/                          # [Создать] Фронтенд Telegram Mini App (SPA)
│   ├── index.html                   # Одностраничное приложение
│   ├── css/
│   │   └── styles.css               # Адаптивные стили с переменными Telegram Theme
│   ├── js/
│   │   ├── telegram.js              # Инициализация Telegram.WebApp
│   │   ├── api.js                   # REST API клиент (автоподстановка X-Telegram-Init-Data)
│   │   ├── geo.js                   # HTML5 Geolocation API (getCurrentPosition)
│   │   ├── student_view.js          # Карточки пар, таймеры, кнопка чекина
│   │   └── starosta_view.js         # Интерактивная шахматка, оверрайды, лок, экспорт
│   └── assets/
│       └── icons/                   # Иконки статусов и логотип
│
├── data/                            # [Создать] Каталог данных (в .gitignore)
│   ├── database.sqlite              # Файл базы данных SQLite
│   ├── seeds/                       # JSON/CSV со списком группы 240326 и расписанием
│   └── backups/                     # Архивы SQLite Online Backup
│
├── tests/                           # [Создать] Тестовый комплекс (pytest)
│   ├── __init__.py
│   ├── conftest.py                  # Фикстуры SQLite in-memory, TestClient, моки Telegram
│   ├── unit/
│   │   ├── test_haversine.py        # Тесты расчета расстояния и фильтрации accuracy
│   │   ├── test_security.py         # Тесты HMAC-SHA256 валидации initData
│   │   └── test_excel_generator.py  # Тесты генерации .xlsx рапортички
│   ├── integration/
│   │   ├── test_api_auth.py         # Тесты эндпоинтов авторизации и RBAC
│   │   ├── test_api_checkin.py      # Тесты сценариев чекина (внутри/вне радиуса, окна времени)
│   │   └── test_api_grid.py         # Тесты шахматки и ручных оверрайдов
│   └── e2e/
│       └── test_full_flow.py        # Сквозной тест онбординг -> чекин -> шахматка -> отчет
│
└── docs/                            # [Существует] Проектная документация
    ├── API.md
    ├── ARCHITECTURE.md
    ├── DATABASE.md
    ├── DEPLOYMENT.md
    └── SRS.md
```

---

## 6. Резюме и рекомендации для Orchestrator

1. **Готовность документации:**  
   Документация (`docs/`) исключительно подробная, высококачественная и полностью согласованная. Все таблицы, формулы, DDL, эндпоинты и схемы взаимодействия определены.
2. **Готовность кода:**  
   Код в репозитории на данный момент **полностью отсутствует (0%)**. Предстоит реализация с чистого листа.
3. **Окружение Python:**  
   Системный Python — 3.9.6. На машине доступен `uv` 0.11.31, который позволяет развернуть чистое виртуальное окружение с Python 3.11 (`uv venv --python 3.11 .venv`) и установить все зависимости мгновенно.
4. **Необходимые дополнения до старта кодинга:**
   - Создать файл вайтлиста студентов группы 240326 «Матинф» БГПУ (28 человек, 2 подгруппы).
   - Создать сид расписания занятий БГПУ с координатами корпусов.
   - Добавить `pytest` и `pytest-asyncio` в зависимости проекта.
   - Инициализировать Git-репозиторий и настроить `.gitignore`.
